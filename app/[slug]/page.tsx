import type{Metadata}from"next";import Link from"next/link";import{notFound}from"next/navigation";import{Breadcrumb,Disclosure,Table}from"../components";import{bySlug,ORIGIN,pages}from"../data";
export const dynamicParams=false;export function generateStaticParams(){return pages.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,p=bySlug.get(slug);if(!p)return{};const url=`${ORIGIN}/${slug}/`;return{title:p.title,description:p.description,alternates:{canonical:url},openGraph:{title:p.title,description:p.description,url,type:"article"}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params,p=bySlug.get(slug);if(!p)notFound();const trouble=p.kind==="troubleshooting";return <main><article><Breadcrumb p={p}/><p className="eyebrow">{p.kind}</p><h1>{p.h1}</h1><p className="dek">{p.answer}</p><div className="trust"><b>{trouble?"Practical guidance":"Evidence-led"}</b><span>Last fact-checked 22 August 2026</span><Link href="/how-we-test-vpns/">Methodology</Link></div>{p.commercial&&<Disclosure/>}{p.commercial&&<div className="verdict"><div><b>Decision point</b><p>Review evidence and limitations before checking current terms.</p></div><Link className="button" rel="sponsored nofollow" href={p.slug.includes("blocker")?"/go/adguard/":p.slug.includes("dns")?"/go/adguard-dns/":"/go/adguard-vpn/"}>Check current price</Link></div>}<section><h2>{trouble?"Quick checks":"Direct comparison"}</h2>{p.checks&&<ol>{p.checks.map(x=><li key={x}>{x}</li>)}</ol>}<Table type={trouble?"fixes":p.slug==="adguard-products"?"products":"vpn"}/></section><section><h2>{trouble?"Why this happens":"Evidence and limitations"}</h2><p>{trouble?"A VPN adds a tunnel, DNS handling and a new route. Change one variable at a time so the failed layer becomes clear.":"Official documentation establishes plan limits, not speed, reliability, privacy performance or access to a streaming service."}</p><ul><li>No exact UK price without a dated checkout capture.</li><li>No speed, latency, leak or streaming result is invented.</li><li>A VPN does not make a person anonymous or unlawful conduct legal.</li></ul></section>{trouble&&<section><h2>When the provider may be the problem</h2><p>If several devices and networks fail, check official service status and support. Resolve the fault before switching provider.</p></section>}<aside className="related"><h2>Related guides</h2>{[...new Set(p.related)].map(x=><Link key={x} href={x}>{bySlug.get(x.replaceAll("/",""))?.h1||x}</Link>)}</aside><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify({"@context":"https://schema.org","@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"Home",item:`${ORIGIN}/`},{"@type":"ListItem",position:2,name:p.h1,item:`${ORIGIN}/${p.slug}/`}]})}}/></article></main>}
