import type{Metadata}from"next";import Link from"next/link";import{notFound}from"next/navigation";const map={adguard:["AdGuard Ad Blocker","/adguard-ad-blocker-review/"],"adguard-vpn":["AdGuard VPN","/adguard-vpn-review-uk/"],"adguard-dns":["AdGuard DNS","/adguard-dns-review/"]}as const;export const dynamicParams=false;export function generateStaticParams(){return Object.keys(map).map(product=>({product}))}export const metadata:Metadata={title:"Affiliate destination pending",robots:{index:false,follow:false}};export default async function Go({params}:{params:Promise<{product:keyof typeof map}>}){const{product}=await params,r=map[product];if(!r)notFound();return <main><article><p className="eyebrow">Central affiliate placeholder</p><h1>{r[0]} destination not yet connected</h1><p className="dek">The approved destination can be added centrally later. No external affiliate URL or tracking ID is present.</p><Link className="button" href={r[1]}>Return to the review</Link></article></main>}
